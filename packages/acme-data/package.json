{
    "name": "@acme/data",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
      "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
      "docker:compose:up": "docker-compose --env-file .env.example -f docker/docker-compose.yaml up",
      "docker:compose:destroy": "docker-compose -f docker/docker-compose.yaml down -v --remove-orphans"
    }
  }