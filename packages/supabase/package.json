{
    "name": "@acme/supabase",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
      "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
      "docker:run": "cd src/docker && cp.env.example.env && docker-compose up"
    }
  }